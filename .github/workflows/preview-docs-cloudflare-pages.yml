name: Docs

on:
  pull_request:
    paths:
      - "docs/**"
      - ".github/workflows/preview-docs-cloudflare-pages.yml"

concurrency:
  group: preview-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  preview:
    # Skip fork PRs — secrets are not available and the deploy step would fail
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v6
        with:
          sparse-checkout: docs

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: docs/pnpm-lock.yaml

      - run: pnpm install --frozen-lockfile
        working-directory: ./docs

      - run: pnpm run build
        working-directory: ./docs

      - name: Get timestamp
        id: timestamp
        run: echo "time=$(date -u +'%b %-d, %Y %-I:%M%p')" >> "$GITHUB_OUTPUT"

      - name: Deploy preview
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy docs/build --project-name=typeorm --branch=${{ github.head_ref }}

      - name: Comment preview URL
        if: success()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: docs-preview
          message: |
            ### <img alt="Cloudflare Pages" src="https://user-images.githubusercontent.com/23264/106598434-9e719e00-654f-11eb-9e59-6167043cfa01.png" width="16"> Documentation Preview

            | Deployment | Actions | Updated (UTC) |
            |------------|---------|---------------|
            | ✅ Ready | <a href="${{ steps.deploy.outputs.deployment-url }}">Preview</a> | ${{ steps.timestamp.outputs.time }} |

      - name: Comment deployment failure
        if: failure()
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: docs-preview
          message: |
            ### <img alt="Cloudflare Pages" src="https://user-images.githubusercontent.com/23264/106598434-9e719e00-654f-11eb-9e59-6167043cfa01.png" width="16"> Documentation Preview

            | Deployment | Actions | Updated (UTC) |
            |------------|---------|---------------|
            | ❌ Failed | <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">Logs</a> | ${{ steps.timestamp.outputs.time }} |
